export default function quickSort(array, element){
    //console.log(array.slice(0,5).map(coin => coin.element));

    let map = new Map()

    map = array.slice(0,25).map()

    for(let i = 0; i < map.size; i++){
        for(let j = 0; j < map.size - i - 1; j++){
             if(map.get){
                 
             }
        }
    }
}
